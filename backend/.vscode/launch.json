{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Debug backend",
            "program": "${workspaceFolder}/app/src/main.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/app"
        }
    ]
}